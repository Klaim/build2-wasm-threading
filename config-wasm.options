config.cxx = em++
config.c = emcc

config.cc.poptions = -pthread -DBOOST_HAS_PTHREADS
config.cc.loptions = -pthread -s PTHREAD_POOL_SIZE=8 -s EXIT_RUNTIME=1

config.test.runner = node --experimental-wasm-threads
